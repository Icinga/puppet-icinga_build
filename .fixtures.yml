fixtures:
  repositories:
    jenkins: https://github.com/jenkinsci/puppet-jenkins.git

  forge_modules:
    archive: "puppet/archive"
    apt:     "puppetlabs/apt"
    concat:  "puppetlabs/concat"
    java:    "puppetlabs/java"
    stdlib:  "puppetlabs/stdlib"
    #jenkins: "rtyler/jenkins"

  symlinks:
    icinga_build: "#{source_dir}"

define icinga_build::config::categorized_view(
  $name,
  $description = '',
  $columns = ['Status', 'org.foo.irgendwas'],
) {
  concat::fragment { 

define icinga_build::config::view(
  $regex = ".*"kkj
